#!/usr/bin/env bash

if [ ! -z "${DEV_BASE// }" ]
then
  cd $DEV_BASE/py/alertz
  
  blListGen \
    -d ../alertz \
    -1L  \
    -u $DVCZ_UDIR \
    -X logs \
    -X .ssh \
    $@
  
else
  echo "DEV_BASE is not defined"
fi
